# 代码转论文智能体 - 项目开发规范
本文件用于约束Claude Code的开发行为，所有开发必须遵守以下规则

## 项目目录结构
/├── backend/ # 后端 FastAPI 服务│ ├── api/ # API 接口定义│ ├── agents/ # 多智能体模块（代码解析 / 实验分析 / 论文生成）│ ├── utils/ # 工具函数（文件处理 / 隐私清理）│ └── main.py # 服务入口├── frontend/ # 前端 Vue3 + Element Plus│ ├── src/ # 前端源码│ └── public/ # 静态资源└── docs/ # 项目文档

## 技术栈
后端：Python + FastAPI
前端：Vue3 + Element Plus
数据库：MySQL + Redis
AI：豆包大模型API
优先复用开源模块：RepoAgent、RD-Agent，禁止从零实现这些成熟能力

## MVP开发范围（严禁超出）
1. 优先支持计算机类项目，先跑通最小闭环
2. 禁止在MVP阶段开发超出需求的功能，先保证核心流程可运行

**开发顺序**
1. 优先开发后端核心API，保证端到端流程可跑通
2. 再开发前端交互界面，保证用户体验

## 代码规范
1. 严格遵循项目现有配置文件与目录结构
2. 统一接口返回格式、异常处理逻辑
3. 复用现有工具类，禁止重复编写通用代码
4. 代码简洁、注释清晰，符合flake8/prettier规范

## 指令要求
1. 每次开发指令注明：遵循CLAUDE.md规范，不超出MVP范围
2. 生成代码后同步做基础自测，修复阻断性BUG
3. 对接第三方API时，使用官方正规接口，严禁违规爬虫
4. 敏感配置不写入代码，统一从.env环境变量读取
5. 接遵循对应平台开放平台规则，适配业务逻辑

